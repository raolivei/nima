---
apiVersion: v1
kind: Secret
metadata:
  name: nima-secrets
  namespace: nima
type: Opaque
stringData:
  # Add any secrets here if needed in the future
  # For now, model paths are configured via environment variables

